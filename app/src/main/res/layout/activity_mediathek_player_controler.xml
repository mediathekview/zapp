<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	android:layout_width="match_parent"
	android:layout_height="wrap_content"
	android:layout_gravity="bottom"
	android:background="@color/black_overlay"
	android:layoutDirection="ltr"
	android:orientation="vertical"
	tools:ignore="Overdraw">

	<LinearLayout
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:gravity="center"
		android:orientation="horizontal"
		android:paddingTop="4dp">

		<ImageButton
			android:id="@id/exo_prev"
			style="@style/ExoMediaButton.Previous"
			tools:ignore="ContentDescription" />

		<ImageButton
			android:id="@id/exo_rew"
			style="@style/ExoMediaButton.Rewind"
			tools:ignore="ContentDescription" />

		<ImageButton
			android:id="@id/exo_play"
			style="@style/ExoMediaButton.Play"
			tools:ignore="ContentDescription" />

		<ImageButton
			android:id="@id/exo_pause"
			style="@style/ExoMediaButton.Pause"
			tools:ignore="ContentDescription"
			tools:visibility="gone" />

		<ImageButton
			android:id="@id/exo_ffwd"
			style="@style/ExoMediaButton.FastForward"
			tools:ignore="ContentDescription" />

		<ImageButton
			android:id="@+id/btn_caption_enable"
			style="@style/ExoMediaButton"
			android:alpha="0.4"
			android:contentDescription="@string/action_enable_subtitles"
			android:src="@drawable/ic_closed_caption_white_24dp" />

		<ImageButton
			android:id="@+id/btn_caption_disable"
			style="@style/ExoMediaButton"
			android:contentDescription="@string/action_disable_subtitles"
			android:src="@drawable/ic_closed_caption_white_24dp"
			android:visibility="gone" />

	</LinearLayout>

	<LinearLayout
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:layout_marginTop="4dp"
		android:gravity="center_vertical"
		android:orientation="horizontal">

		<TextView
			android:id="@id/exo_position"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:includeFontPadding="false"
			android:paddingLeft="4dp"
			android:paddingRight="4dp"
			android:textColor="#FFBEBEBE"
			android:textSize="14sp"
			android:textStyle="bold" />

		<com.google.android.exoplayer2.ui.DefaultTimeBar
			android:id="@id/exo_progress"
			android:layout_width="0dp"
			android:layout_height="26dp"
			android:layout_weight="1" />

		<TextView
			android:id="@id/exo_duration"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:includeFontPadding="false"
			android:paddingLeft="4dp"
			android:paddingRight="4dp"
			android:textColor="#FFBEBEBE"
			android:textSize="14sp"
			android:textStyle="bold" />

	</LinearLayout>

</LinearLayout>
